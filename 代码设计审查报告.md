# 代码设计审查报告

> 生成日期：2026-07-30
> 范围：src/ 下全部源码

---

## 优先级 P0 — 必须改

### 1. `kobot.ts` 上帝类（1344 行），违反单一职责

**问题**
[src/kobot.ts](file:///Users/kye/Documents/AI-NEW/kobot/src/kobot.ts) 同时负责：

- 事件系统（定义两套流事件类型）
- Session 管理
- 请求队列
- 工具执行编排
- 与 Agent、ACR、ProgressGuard、SkillManager 的粘合
- CLI 启动、Webhook、飞书通道启动

`Kobot` 类是一个典型的上帝类，1344 行的单文件导致：

- 难以测试：大多数方法依赖类的内部状态
- 难以理解：一个方法（如 `run()`）横跨多个关注点
- 难以修改：改动任何子系统都可能影响整个类

**整改方案**
拆分为以下专注模块：

| 模块                         | 职责                                         | 参考行数 |
| ---------------------------- | -------------------------------------------- | -------- |
| `src/kobot/event-bus.ts`     | 统一事件系统，消除两套事件类型               | ~80      |
| `src/kobot/run-loop.ts`      | Agent 运行编排（从 `run()`/`stream()` 抽取） | ~200     |
| `src/kobot/request-queue.ts` | 请求队列逻辑                                 | ~60      |
| `src/kobot/channels.ts`      | CLI、Webhook、飞书通道的启动逻辑             | ~150     |
| `src/kobot.ts`               | 顶层协调类，保持薄层                         | ~400     |

---

### 3. 两套流事件系统

**问题**

- [src/ai/types.ts](file:///Users/kye/Documents/AI-NEW/kobot/src/ai/types.ts#L134-L215) 定义了低层级 SSE 流事件（`TextDeltaEvent`, `ToolCallDeltaEvent` 等 10+ 种事件）
- [src/kobot.ts](file:///Users/kye/Documents/AI-NEW/kobot/src/kobot.ts#L26-L72) 定义了另一套业务层流事件（`STREAM_EVENT_TEXT_DELTA`, `STREAM_EVENT_TOOL_STARTED` 等字符串常量）

中间通过 `agent.ts` 的 `message_update` 事件嵌套传递低层级事件，形成了晦涩的事件传递链：

```
AI stream → low-level StreamEvent → agent.ts message_update → kobot.ts 重新映射 → 外部
```

**整改方案**
在 Agent 层直接负责事件映射，对外统一暴露一套事件体系（使用 `agent/types.ts` 中的 `AgentEvent` 体系），`kobot.ts` 不再定义独立的事件类型。

---

## 优先级 P1 — 应该改

### 4. `AgentContextRuntime` 过于复杂（~1040 行）

**问题**
[src/context-runtime/runtime.ts](file:///Users/kye/Documents/AI-NEW/kobot/src/context-runtime/runtime.ts) 同时负责：

- 上下文压缩（5 层策略：L1-L5）
- Token 监控
- 密度监控
- 工作区观察
- 状态提取
- 快照构建
- 工具摘要
- 会话记忆
- 可观测性指标
- 冷却机制
- 预算控制
- 事件系统

严重违反单一职责原则。部分功能（如语义分析、状态追踪）与 [src/progress-guard/index.ts](file:///Users/kye/Documents/AI-NEW/kobot/src/progress-guard/index.ts) 中的功能重叠。

**整改方案**
拆分为独立关注点：

| 模块                                   | 职责                           |
| -------------------------------------- | ------------------------------ |
| `src/context-runtime/compressor.ts`    | 上下文压缩逻辑                 |
| `src/context-runtime/token-monitor.ts` | Token 预算监控                 |
| `src/context-runtime/snapshot.ts`      | 快照构建                       |
| `src/context-runtime/observability.ts` | 可观测性指标                   |
| `src/context-runtime/runtime.ts`       | 只保留调度协调，精简到 ~300 行 |

---

## 总结

| 优先级 | 问题              | 影响面 | 工作量 | 建议执行顺序 |
| ------ | ----------------- | ------ | ------ | ------------ |
| P0     | `kobot.ts` 上帝类 | 高     | 大     | 1            |
| P0     | 两套流事件系统    | 高     | 中     | 2            |
| P1     | ACR 过于复杂      | 中     | 大     | 3            |
